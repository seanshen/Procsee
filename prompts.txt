HttpSession to a Redis cache by using Spring (framework) Session backed by Redis cache. For testing I need to run three web server instances on three diffrent HTTP listening ports. 2) A reverse proxy written in Node.js. For a same uer session, identified by JSESSIONID, the proxy roundrobbin between the three web server instances. 3) A PostMan collection that I can load into PostMan to run tests. A test is successful if it makes the first call to set the testAttr, makes the 2nd call to get and validate testAttr, makes the 3rd call to set testAttr to a different value, and then makes the 4th call to get and validate the value. 
